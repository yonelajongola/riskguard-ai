# Local backend origin. A bare origin is normalized to include /api.
VITE_API_BASE_URL=http://localhost:5000
# Portfolio-only demo data and public workspace creation are opt-in.
VITE_ENABLE_DEMO_MODE=true
VITE_ENABLE_REGISTRATION=true
